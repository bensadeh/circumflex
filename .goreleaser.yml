version: 2
before:
  hooks:
    - go mod download
builds:
  - main: ./cmd/clx
    binary: clx
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
archives:
  - files:
      - share/man/*
checksum:
  name_template: 'checksums.txt'
snapshot:
  name_template: "{{ .Tag }}-next"
